import { styled } from '@ratex-ui/react'

export const LoaderContainer = styled('div', {
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
  height: '100%',
  paddingBottom: '$20',
})
